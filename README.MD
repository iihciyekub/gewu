

# 快捷键 cmd 对应windows的 ctrl
在文件项目栏 按上下可以快速切换文件
在文件项目栏,按左右调整文件的位置顺序

cmd+e -- 切换md / data table视图
cmd+shift+f -- 隐藏显示pdf预览窗口
cmd+shift+s -- 隐藏显示文件项目栏

## 启动方式
优先使用 Node 启动（跨平台）：

- `node start.js`

兼容旧的脚本入口：

- `./start.sh`
