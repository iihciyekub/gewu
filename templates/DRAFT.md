Draft here

- `\cite{<doi>}`
- - 在段落句首插入文献引用，

- `\citep{<doi>}` 
- - 在段落语句未尾插入带括号的文献引用。


- `\groupby{<groupName>}{<keyField>}`
- - 按照指定字段对文献进行分组，生成分组统计结果。

- `\json{<fieldPath>}{<groupName>}`
- - 查询 JSON 数据项，返回指定字段值和分组信息（需要 meta_info.doi 字段支持）。

\ref