# 双击编辑功能使用指南

## 功能概述

现在支持双击表格单元格直接编辑JSON数据，并自动保存到服务器。

## 使用方法

### 1. 双击编辑
- 在**分层视图**中，找到任意数据字段
- **双击**字段值区域
- 弹出编辑对话框

### 2. 编辑对话框

对话框包含以下可编辑内容：

#### 基本信息
- **字段名称**: 只读显示，标识当前编辑的字段
- **值**: 可编辑的字段内容（支持文本、JSON等）

#### PDF位置信息（可选）
- **页码标签**: PDF中显示的页码（如"2044"）
- **页面索引**: 实际页面编号（从1开始，如7）
- **引用文本**: PDF中的原文引用

### 3. 保存更改
- 点击 **💾 保存** 按钮
- 或按 **Ctrl+Enter** 快捷键
- 系统会自动保存到JSON文件
- 显示保存成功提示
- 自动刷新页面显示

### 4. 取消编辑
- 点击 **取消** 按钮
- 或点击对话框外的遮罩
- 或按 **ESC** 键

## 功能特点

### ✅ 自动保存
- 点击保存后立即写入JSON文件
- 无需手动刷新页面
- 显示Toast提示反馈

### ✅ 智能识别
- 自动检测JSON格式
- 支持字符串、数字、对象等类型
- 保留原有数据结构

### ✅ PDF关联
- 同时编辑PDF位置信息
- 自动生成跳转链接
- 支持页码和引用文本

### ✅ 视觉提示
- 鼠标悬停显示"✏️ 双击编辑"提示
- 高亮显示可编辑区域
- 平滑的动画效果

## 快捷键

- **双击**: 打开编辑对话框
- **Ctrl+Enter**: 保存并关闭对话框
- **ESC**: 取消并关闭对话框

## 注意事项

1. **JSON格式**: 如果值是JSON对象，请保持正确的JSON格式
2. **PDF位置**: 页面索引从1开始，不是从0开始
3. **自动保存**: 保存后会立即写入文件，请谨慎操作
4. **必需字段**: 页码标签和页面索引必须同时填写才能生成PDF链接

## 示例

### 编辑普通字段
1. 双击 "Model Type" 的值
2. 修改为 "Staggered DID (交错双重差分)"
3. 点击保存
4. ✅ 自动保存并刷新

### 添加PDF位置
1. 双击任意字段值
2. 填写：
   - 页码标签: "2044"
   - 页面索引: 7
   - 引用文本: "observe the implementation year..."
3. 保存
4. ✅ 字段旁自动出现📄图标链接

## 兼容性

- ✅ 支持所有现有JSON格式
- ✅ 保持与旧数据的兼容性
- ✅ 自动转换数据结构
- ✅ 跨浏览器支持
