# 编辑模式使用指南

## 功能概览

现在系统支持完整的JSON数据编辑功能，包括：

### 1. 切换编辑模式
- 点击中间面板的 **编辑按钮** (✏️) 进入编辑模式
- 或在视图模式下拉菜单中选择"编辑模式"

### 2. 编辑功能

#### 编辑元信息 (meta_info)
- 直接在输入框中修改DOI、标题、作者等信息
- 修改会实时保存到内存中

#### 编辑类别
- **修改类别标题**: 点击类别标题直接编辑
- **添加项目**: 点击类别右侧的 ➕ 按钮
- **删除类别**: 点击类别右侧的 🗑️ 按钮

#### 编辑项目
每个项目包含以下可编辑字段：
- **Label (标签)**: 显示给用户的中文名称
- **Key (键)**: 字段的唯一标识符（英文）
- **Value (值)**: 字段的实际内容
- **页码标签**: PDF中的页码标签（如"2044"）
- **页面索引**: PDF实际页面索引（从1开始，如7）
- **引用文本**: PDF中引用的原文

#### 添加新类别
- 点击顶部的 ➕ 按钮添加新类别
- 可以自定义类别ID、标题和图标

#### 删除项目
- 点击项目右上角的 ❌ 按钮删除该项目

### 3. 保存数据
- 点击顶部的 **💾 保存** 按钮
- 数据会保存到服务器的 `src/` 目录下对应的JSON文件
- 保存时会自动转换回原始格式，保持与现有文件的兼容性

### 4. PDF跳转
- 在编辑模式下，如果项目有PDF位置信息，会显示 📄 预览 按钮
- 点击可直接跳转到PDF对应页面并高亮显示引用文本

## 数据结构说明

系统使用统一的数据结构：**类别 → 项目**

### 每个项目必须包含：
- `key`: 字段标识符
- `label`: 显示标签
- `value`: 字段值
- `pdf_location` (可选): PDF位置信息
  - `page_label`: 页码标签
  - `pdf_page_index`: 页面索引
  - `pdf_open_params`: 打开参数
  - `quote`: 引用文本

### 类别必须包含：
- `id`: 类别标识符
- `title`: 类别标题
- `icon`: Font Awesome图标
- `items`: 项目数组

## 快捷操作

1. **快速添加项目**: 进入编辑模式 → 选择类别 → 点击 ➕ → 填写信息
2. **批量编辑**: 在编辑模式下可以同时修改多个字段，最后统一保存
3. **PDF关联**: 填写页面索引和引用文本后，系统会自动生成PDF链接

## 注意事项

- 保存前请确保所有必需字段都已填写
- Key字段建议使用英文和下划线（如 `model_type`）
- 页面索引从1开始，不是从0开始
- 修改后记得点击保存按钮，否则数据不会持久化

## 兼容性

系统完全兼容旧的JSON格式，会自动进行双向转换：
- **读取**: 旧格式 → 标准化格式（用于编辑）
- **保存**: 标准化格式 → 旧格式（保持兼容）
